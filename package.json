{
  "name": "360-hospitalar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build",
    "start": "cd backend && node dist/db/migrate.js && node dist/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
